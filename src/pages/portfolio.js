import React from 'react';

import SectionWrapper from '../components/SectionWrapper';

function Portfolio(props)
{
  return (
    <SectionWrapper id="portfolio">
      <p>This is portfolio info</p>
    </SectionWrapper>
  );
}

export default Portfolio;