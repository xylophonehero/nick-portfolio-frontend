import React from 'react';
import SectionWrapper from '../components/SectionWrapper'

function Services(props)
{
  return (
    <SectionWrapper id="services">
      <p>This is services info</p>
    </SectionWrapper>
  );
}

export default Services;