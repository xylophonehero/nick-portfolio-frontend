import React from 'react';

import SectionWrapper from '../components/SectionWrapper';

function Testimonials(props)
{
  return (
    <SectionWrapper id="testimonials">
      <p>This is testimonial info</p>
    </SectionWrapper>
  );
}

export default Testimonials;